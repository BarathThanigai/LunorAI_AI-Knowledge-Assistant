# Concurrent Web Crawler

The project uses **React**, **FastAPI**, **Python**, **asyncio**, **aiohttp**, **BeautifulSoup**, **SQLite**, **Docker**, and **GitHub Actions**.

## Purpose

It is designed for concurrent web crawling.
